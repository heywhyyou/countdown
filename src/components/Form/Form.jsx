const Form = () => {
  return (
    <div>
      <input
        type="datetime-local"
        placeholder="DD.MM.YYYY"
        className="input input-bordered input-secondary w-full max-w-xs mb-5"
      />
    </div>
  );
};

export default Form;
